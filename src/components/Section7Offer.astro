---
// src/components/Section7Offer.astro
const offerDetails = [
    { text: "Run your track through our Pulse Cascade Splitter" },
    { text: "Do the same with a professional reference track of your choice" },
    { text: "Show you exactly what's different between them" },
    { text: "Give you specific, actionable steps to improve your production" },
    { text: "Provide you with the separated stems from your reference track to study" }
];
// Spots will be managed by the shared spots manager

// Student avatars for the 5-star rating display
const studentAvatars = [
    { src: "/student-photos/BigJerr.webp", alt: "BigJerr" },
    { src: "/student-photos/Beatfortress.webp", alt: "Beatfortress" },
    { src: "/student-photos/Elisha.webp", alt: "Elisha" },
    { src: "/student-photos/Kip.webp", alt: "Kip" },
    { src: "/student-photos/1wayout.webp", alt: "1wayout" },
    { src: "/student-photos/DIVOCEAN.webp", alt: "DIVOCEAN" }
];

// Star SVG
const StarIcon = `
<svg class="w-4 h-4 text-yellow-400" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
</svg>
`;
---
<section id="offer-section" class="py-16 md:py-24 bg-gradient-to-b from-[#080810] to-[#0c0c18] px-4 md:px-8">
    <div class="max-w-3xl mx-auto mb-6">
        <h2 class="text-3xl md:text-4xl lg:text-5xl font-extrabold tracking-tight leading-tight text-center pb-3">
            <span class="block text-silvery-gradient pb-2">Stop Wondering What<br class="hidden sm:block"> You're Missing</span>
            <span class="block text-transparent bg-clip-text bg-gradient-to-r from-pulse-purple to-pulse-cyan pb-2 leading-normal">Get the Data That<br class="hidden sm:block"> Changes Everything</span>
        </h2>
        <p class="mt-6 text-lg md:text-xl text-gray-300 text-center">
            Instead of asking you to trust us, we want to <strong class="text-white">prove our method works</strong> by showing you exactly what separates your tracks from Billboard Top 40 hits, for free.
        </p>
    </div>

    <!-- ADD COUNTDOWN TIMER -->
    <div class="max-w-3xl mx-auto mb-8 text-center">
        <div class="bg-red-500/10 border border-red-500/20 rounded-lg p-4 mb-6">
            <p class="text-red-400 font-semibold mb-2">⚠️ LIMITED TIME OFFER</p>
            <p class="text-gray-300 text-sm md:text-base">This complimentary track analysis offer expires in:</p>
            <div id="countdown-timer" class="text-xl md:text-2xl font-bold text-white mt-2 font-mono">
                <span id="days">02</span>:<span id="hours">23</span>:<span id="minutes">45</span>:<span id="seconds">12</span>
            </div>
            <p class="text-xs md:text-sm text-gray-400 mt-2">After this deadline, we'll be charging $297 for the same professional analysis</p>
        </div>
    </div>

    <!-- Offer Card with Subtle Glow -->
    <div class="max-w-3xl mx-auto card-gradient p-8 md:p-10 rounded-2xl shadow-glass border border-gray-800/20 overflow-hidden">
        <div class="relative z-10">
            <h3 class="text-2xl md:text-3xl font-semibold text-transparent bg-clip-text bg-gradient-to-r from-pulse-purple to-pulse-cyan mb-8 text-center pb-2 leading-normal">What You Get in Your Free Analysis Session</h3>
            
            <!-- VALUE STACK WITH PRICING -->
            <div class="bg-[#080810]/60 rounded-xl p-6 mb-8 border border-pulse-purple/20">
                <h4 class="text-xl font-semibold text-white mb-4 text-center">Here's Everything Included in Your Complimentary Session:</h4>
                <ul class="space-y-5 md:space-y-4 mb-6">
                    <li class="flex flex-col sm:flex-row sm:items-start sm:justify-between text-gray-200 text-sm sm:text-md md:text-lg pl-2 gap-2 sm:gap-4">
                        <div class="flex items-start flex-1">
                            <svg class="w-5 h-5 text-pulse-purple mr-3 mt-1 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                            </svg>
                            <span class="leading-relaxed">AI-powered stem separation of your track + professional reference track</span>
                        </div>
                        <span class="text-pulse-cyan font-semibold ml-8 sm:ml-0 sm:flex-shrink-0">$197</span>
                    </li>
                    <li class="flex flex-col sm:flex-row sm:items-start sm:justify-between text-gray-200 text-sm sm:text-md md:text-lg pl-2 gap-2 sm:gap-4">
                        <div class="flex items-start flex-1">
                            <svg class="w-5 h-5 text-pulse-purple mr-3 mt-1 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                            </svg>
                            <span class="leading-relaxed">30-minute live analysis session with professional engineer</span>
                        </div>
                        <span class="text-pulse-cyan font-semibold ml-8 sm:ml-0 sm:flex-shrink-0">$297</span>
                    </li>
                    <li class="flex flex-col sm:flex-row sm:items-start sm:justify-between text-gray-200 text-sm sm:text-md md:text-lg pl-2 gap-2 sm:gap-4">
                        <div class="flex items-start flex-1">
                            <svg class="w-5 h-5 text-pulse-purple mr-3 mt-1 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                            </svg>
                            <span class="leading-relaxed">Custom written action plan with 3 specific, measurable fixes</span>
                        </div>
                        <span class="text-pulse-cyan font-semibold ml-8 sm:ml-0 sm:flex-shrink-0">$197</span>
                    </li>
                    <li class="flex flex-col sm:flex-row sm:items-start sm:justify-between text-gray-200 text-sm sm:text-md md:text-lg pl-2 gap-2 sm:gap-4">
                        <div class="flex items-start flex-1">
                            <svg class="w-5 h-5 text-pulse-purple mr-3 mt-1 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                            </svg>
                            <span class="leading-relaxed">Separated stems from your reference track for detailed study</span>
                        </div>
                        <span class="text-pulse-cyan font-semibold ml-8 sm:ml-0 sm:flex-shrink-0">$97</span>
                    </li>
                    <li class="flex flex-col sm:flex-row sm:items-start sm:justify-between text-gray-200 text-sm sm:text-md md:text-lg pl-2 gap-2 sm:gap-4">
                        <div class="flex items-start flex-1">
                            <svg class="w-5 h-5 text-pulse-purple mr-3 mt-1 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                            </svg>
                            <span class="leading-relaxed"><strong>BONUS:</strong> "Quick Wins" preset pack for instant improvements</span>
                        </div>
                        <span class="text-pulse-cyan font-semibold ml-8 sm:ml-0 sm:flex-shrink-0">$147</span>
                    </li>
                </ul>
                
                <div class="border-t border-gray-700 pt-4 flex justify-between items-center">
                    <span class="text-lg font-semibold text-gray-300">Total Value:</span>
                    <div class="text-right">
                        <div class="text-lg text-gray-400 line-through">$935</div>
                        <div class="text-3xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-pulse-purple to-pulse-cyan pb-1 leading-normal">FREE</div>
                    </div>
                </div>
            </div>
            
            <div class="text-center mb-10">
                <p class="text-3xl md:text-4xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-pulse-purple to-pulse-cyan pb-1 leading-normal">Your Investment: $0</p>
                <p class="text-md text-gray-400 mt-1">(But Only If You Act Now)</p>
            </div>

            <!-- VALUE PROMISE -->
            <div class="bg-green-500/10 border border-green-500/20 rounded-lg p-6 mb-8">
                <div class="flex items-center justify-center mb-4">
                    <div class="bg-green-500/20 p-3 rounded-full">
                        <svg class="w-8 h-8 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
                        </svg>
                    </div>
                </div>
                <h4 class="text-xl font-semibold text-green-400 text-center mb-3">Our Commitment</h4>
                <p class="text-gray-300 text-center">
                    You'll walk away with at least 3 specific, actionable improvements for your track. <strong>That's our standard for every session.</strong>
                </p>
            </div>

            <p class="mb-8 text-lg text-gray-300 text-center">
                You'll walk away with <span class="text-pulse-purple font-semibold">3 specific, measurable changes</span> that will immediately show you what needs fixing in your productions. <span class="text-pulse-cyan font-semibold">Pure insights. Actionable data. The exact roadmap your tracks need.</span>
            </p>

            <div class="mb-8 text-center">
                <p class="text-xl md:text-2xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-pulse-purple to-pulse-cyan pb-1 leading-normal">
                    Pure analysis. Zero sales pressure.
                </p>
                <p class="mt-2 text-lg text-gray-300">The data your tracks need to compete with chart hits.</p>
            </div>

            <div class="text-center">
                <button id="open-calendar-modal-offer" 
                      class="btn-lg shadow-cta-glow text-lg inline-block font-bold transition-all duration-300">
                   CLAIM MY FREE TRACK ANALYSIS ($935 VALUE)
                </button>
                <p class="mt-4 text-sm text-gray-400">
                    *Only <span id="spots-left-offer" class="text-pulse-cyan font-semibold">- spots left as of loading...</span>*
                </p>
            </div>
            
            <!-- Interactive Review Section with Student Thumbnails -->
            <div class="mt-10 flex flex-col items-center justify-center bg-[#080810]/60 py-4 px-4 rounded-xl border border-gray-800/30">
                <div class="flex items-center justify-center">
                    <div class="flex items-center">
                        {[...Array(5)].map(() => <Fragment set:html={StarIcon} />)}
                        <span class="ml-2 text-sm font-semibold text-white">4.9 | Based on 127 students</span>
                    </div>
                    <div class="flex ml-3 -space-x-3 avatar-group">
                        {studentAvatars.map((avatar, index) => (
                            <div 
                                class="avatar-item w-7 h-7 rounded-full border-2 border-[#070710] overflow-hidden hover:z-10 relative transition-transform duration-200" 
                                style={`z-index: ${6-index}`}
                            >
                                <img src={avatar.src} alt={avatar.alt} class="w-full h-full object-cover" loading="lazy" />
                            </div>
                        ))}
                    </div>
                </div>
                
                <!-- Live Activity Feed -->
                <div id="live-activity" class="mt-4 w-full max-w-md">
                    <div class="bg-green-500/10 border border-green-500/20 rounded-lg p-3">
                        <div class="flex items-center justify-between">
                            <div class="flex items-center">
                                <div class="w-2 h-2 bg-green-400 rounded-full animate-pulse mr-2"></div>
                                <span class="text-green-400 text-xs font-semibold">LIVE ACTIVITY</span>
                            </div>
                            <span class="text-xs text-gray-400" id="activity-time">2 min ago</span>
                        </div>
                        <p class="text-xs text-gray-300 mt-1" id="activity-message">
                            🎵 Sarah from Los Angeles just booked her session
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Calendar Modal -->
    <div id="calendar-modal" class="fixed inset-0 bg-black/80 backdrop-blur-md flex items-center justify-center p-1 md:p-4 z-[100] hidden transition-opacity duration-300 opacity-0">
        <div class="glassmorphism p-2 md:p-6 lg:p-8 rounded-xl max-w-2xl w-full text-center relative shadow-2xl mx-1 md:mx-0">
            <button id="close-calendar-modal" class="absolute top-2 right-2 md:top-4 md:right-4 text-gray-400 hover:text-white transition-colors z-10">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6 md:w-8 md:h-8">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </button>
            <h3 class="text-lg md:text-2xl font-semibold text-gray-100 mb-2 md:mb-4 px-2">Book Your 'Instant Song Upgrade' Session</h3>
            <p class="text-sm md:text-base text-gray-300 mb-2 md:mb-4 px-2">Complete the form below to secure your spot for a free track analysis session.</p>
            <div id="iframe-container" class="w-full">
                <iframe id="ghl-iframe" style="border:none;width:100%;min-height:500px;" scrolling="no" title="Instant Song Upgrade Session" class="rounded-md"></iframe>
            </div>
        </div>
    </div>
</section>

<style>
    .student-review-avatar {
        box-shadow: 0 0 10px rgba(0,0,0,0.2);
        cursor: pointer;
    }
    
    .student-review-avatar:hover {
        border-color: rgba(139, 92, 246, 0.8);
        box-shadow: 0 0 15px rgba(139, 92, 246, 0.5);
    }
    
    /* Avatar hover effects */
    .avatar-item {
        transition: all 0.2s ease;
    }
    
    .avatar-item:hover {
        transform: translateY(-5px) scale(1.15);
    }
    
    .avatar-group:hover .avatar-item:not(:hover) {
        transform: translateY(2px);
    }
    
    /* Mobile-specific iframe styling */
    @media (max-width: 768px) {
        #ghl-iframe {
            min-height: 450px !important;
            border-radius: 8px;
        }
        
        #iframe-container {
            padding: 0;
            margin: 0;
        }
        
        /* Ensure modal takes full mobile width */
        #calendar-modal .glassmorphism {
            margin: 4px;
            max-height: 95vh;
            overflow-y: auto;
        }
        
        /* Hide any potential scrollbars on mobile */
        #calendar-modal .glassmorphism::-webkit-scrollbar {
            display: none;
        }
        
        #calendar-modal .glassmorphism {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }
    }
</style>

<script>
    const openButtonOffer = document.getElementById('open-calendar-modal-offer');
    const calendarModalInstance = document.getElementById('calendar-modal'); 
    const closeButtonCalendar = document.getElementById('close-calendar-modal');

    if (openButtonOffer && calendarModalInstance) {
        openButtonOffer.addEventListener('click', () => {
            calendarModalInstance.classList.remove('hidden');
            setTimeout(() => {
                calendarModalInstance.classList.add('opacity-100');
                // Load iframe source with mobile-specific handling
                const iframe = document.getElementById('ghl-iframe') as HTMLIFrameElement;
                if (iframe && !iframe.src) {
                    console.log('Loading GHL iframe...');
                    
                    // Detect mobile device
                    const isMobile = window.innerWidth <= 768 || /Android|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent);
                    
                    if (isMobile) {
                        // Mobile: Force clean form load with longer delay and cache busting
                        setTimeout(() => {
                            const timestamp = Date.now();
                            iframe.src = `https://api.leadconnectorhq.com/widget/survey/6RBmf85SJ0IFIO51kKfd?t=${timestamp}&mobile=1`;
                            iframe.id = '6RBmf85SJ0IFIO51kKfd';
                            
                            // Additional mobile-specific handling
                            iframe.onload = () => {
                                console.log('Mobile iframe loaded successfully');
                                // Force a refresh if needed
                                setTimeout(() => {
                                    if (iframe.contentWindow) {
                                        try {
                                            iframe.contentWindow.postMessage('refresh', '*');
                                        } catch (e) {
                                            console.log('Cross-origin iframe, cannot refresh');
                                        }
                                    }
                                }, 1000);
                            };
                        }, 1000); // Longer delay for mobile
                    } else {
                        // Desktop: Standard loading
                        setTimeout(() => {
                            iframe.src = 'https://api.leadconnectorhq.com/widget/survey/6RBmf85SJ0IFIO51kKfd';
                            iframe.id = '6RBmf85SJ0IFIO51kKfd';
                        }, 300);
                    }
                }
            }, 100);
        });
    }
    if (closeButtonCalendar && calendarModalInstance) {
        closeButtonCalendar.addEventListener('click', () => {
            calendarModalInstance.classList.remove('opacity-100');
            setTimeout(() => calendarModalInstance.classList.add('hidden'), 300);
        });
    }
    window.addEventListener('keydown', (event) => {
        if (event.key === 'Escape' && calendarModalInstance && !calendarModalInstance.classList.contains('hidden')) {
            closeButtonCalendar?.click();
        }
    });
    if (calendarModalInstance) {
        calendarModalInstance.addEventListener('click', (event) => {
            if (event.target === calendarModalInstance) {
                closeButtonCalendar?.click();
            }
        });
    }

    // Load GoHighLevel form script and log when it loads
    const ghlScript = document.createElement('script');
    ghlScript.src = 'https://link.msgsndr.com/js/form_embed.js';
    ghlScript.async = true;
    ghlScript.onload = function() {
        console.log('GHL script loaded successfully');
    };
    ghlScript.onerror = function() {
        console.error('Failed to load GHL script');
    };
    document.head.appendChild(ghlScript);

    // COUNTDOWN TIMER FUNCTIONALITY
    function startCountdown() {
        // Set countdown to 2 days, 23 hours, 45 minutes from now
        const countdownDate = new Date().getTime() + (2 * 24 * 60 * 60 * 1000) + (23 * 60 * 60 * 1000) + (45 * 60 * 1000);
        
        const timer = setInterval(function() {
            const now = new Date().getTime();
            const distance = countdownDate - now;
            
            const days = Math.floor(distance / (1000 * 60 * 60 * 24));
            const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1000);
            
            const daysEl = document.getElementById('days');
            const hoursEl = document.getElementById('hours');
            const minutesEl = document.getElementById('minutes');
            const secondsEl = document.getElementById('seconds');
            
            if (daysEl) daysEl.textContent = days.toString().padStart(2, '0');
            if (hoursEl) hoursEl.textContent = hours.toString().padStart(2, '0');
            if (minutesEl) minutesEl.textContent = minutes.toString().padStart(2, '0');
            if (secondsEl) secondsEl.textContent = seconds.toString().padStart(2, '0');
            
            if (distance < 0) {
                clearInterval(timer);
                // Reset to a new countdown when it expires
                setTimeout(startCountdown, 1000);
            }
        }, 1000);
    }

    // Start the countdown when the page loads
    document.addEventListener('DOMContentLoaded', startCountdown);

    // Initialize spots count when page loads
    document.addEventListener('DOMContentLoaded', () => {
        if ((window as any).spotsManager) {
            (window as any).spotsManager.updateSpotsWithDate('spots-left-offer');
        }
    });

    // LIVE ACTIVITY FEED
    const activityMessages = [
        "🎵 Sarah from Los Angeles just booked her session",
        "🔥 Marcus from Detroit got his track analyzed", 
        "⚡ Emma from London just claimed her spot",
        "🎯 Jake from Nashville booked his analysis",
        "🚀 Alex from Berlin just signed up",
        "🎨 Maya from Toronto claimed her session",
        "⭐ Chris from Phoenix just booked",
        "🎼 Sofia from Miami got her analysis slot",
        "🔊 David from Seattle just registered",
        "🎪 Lisa from Chicago claimed her spot"
    ];

    const timeOptions = [
        "2 min ago", "5 min ago", "8 min ago", "12 min ago", "15 min ago",
        "18 min ago", "23 min ago", "28 min ago", "32 min ago", "35 min ago"
    ];

    function updateActivityFeed() {
        const activityMessage = document.getElementById('activity-message');
        const activityTime = document.getElementById('activity-time');
        
        if (activityMessage && activityTime) {
            const randomMessage = activityMessages[Math.floor(Math.random() * activityMessages.length)];
            const randomTime = timeOptions[Math.floor(Math.random() * timeOptions.length)];
            
            // Fade out
            const activityContainer = document.getElementById('live-activity');
            if (activityContainer) {
                activityContainer.style.opacity = '0.5';
                
                setTimeout(() => {
                    activityMessage.textContent = randomMessage;
                    activityTime.textContent = randomTime;
                    
                    // Fade back in
                    activityContainer.style.opacity = '1';
                }, 200);
            }
        }
    }

    // Update activity feed every 8-15 seconds
    function startActivityFeed() {
        setInterval(() => {
            updateActivityFeed();
        }, Math.random() * 7000 + 8000); // Random interval between 8-15 seconds
    }

    // Start activity feed with initial delay
    setTimeout(startActivityFeed, 3000);
</script> 